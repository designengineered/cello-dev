---
const { title } = Astro.props;
---

<section>
	{title && <h2>{title}</h2>}
	<slot />
</section>

<style>
	section {
		max-width: 800px;
		margin: 0 auto 32px;
	}

	h2 {
		font-weight: 700;
	}
	@media (width <= 800px) {
		p,
		h2 {
			font-size: 1.3rem;
		}
	}
</style>
